<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMain" aria-expanded="false" aria-label="Toggle navigation">
      <span class="fa fa-bars navbar-icon"></span>
    </button>
    <a href="/" class="logo">
      <%= image_tag(t(:logo), alt: 'How to Edibles', width: 200, height: 72, class:'logo-small') %>
      <%= image_tag(t(:logo), alt: 'How to Edibles', width: 260, height: 93, class:'logo-regular') %>
    </a>
    <div class="nav-spacer">
    </div>

    <div id="navbarMain" class="navbar-collapse collapse">
      <ul id="nav" class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <%= link_to 'Calculator', '/edible-dosage-calculator', class: 'nav-link' %>
        </li>
        <% @category_list.each do |category| %>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= category.name %></a>
            <div class="dropdown-menu" role="menu">
              <% category.recipes.each do |recipe| %>
                <%= link_to recipe.name, recipe_path(recipe.slug), class: 'dropdown-item' %>
              <% end %>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
